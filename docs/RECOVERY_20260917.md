# 2026-09-17 云端超时恢复

TASK-0028 / PR #4，Subagents: none。

现场群2一条新增行在09:38:30返回ECONNABORTED，原API超时为8秒。状态保留unknown、attempts=1、pending sequence=2、queue=1；连接正常但后续操作被挡住。网络具体设备/路由原因没有证据，不能归因于是否连接公司Wi-Fi。

User要求先尽快恢复。确认云端STOP_VERIFIED后，以20秒超时补试原持久化新增操作，只沿用原UUID、sequence、行ID及原消息，不生成新操作或清空状态。飞书明确成功，ROW_ADDED rows=2，pending=false、queued=0；原消息和卡片保持相同。随后START_READY / CONNECTED，两群均ready，恢复快照各2行，无积压。未重发卡、全卡刷新或模拟员工输入。

## 预防补丁

- API等待从8秒调整为20秒，保存仍通过异步队列，不延长回调即时回应。
- 已发送更新遇到明确的临时网络错误时，持久化重试原因和次数，最多追加两次重试；默认等待1秒、2秒。后续其他行仍可排队，按原顺序处理。
- 每次沿用完全相同的UUID、sequence、行ID和更新正文；只收到code=0才合入已保存状态。UUID/sequence冲突仍保留unknown，绝不将冲突当作成功或递增序号绕过。
- 连续三次网络失败后保留请求及队列并提示维护；不无限重试。权限、参数、证书和未分类错误不按临时网络错误重试。已持久化的重试预算跨重启保留；旧版裸unknown不自动重放。
- 仅修改共享传输、队列与状态验证逻辑；两群使用同一补丁。卡片布局、本人权限、群2AI、09:30、归档和群隔离不变。

依据：[飞书新增组件API](https://open.feishu.cn/document/cardkit-v1/card-element/create)和[官方SDK模型](https://github.com/larksuite/oapi-sdk-go/blob/v3.12.0/service/cardkit/v1/model.go)定义UUID用于同批操作防重。没有据此将重复UUID错误推断为原操作已成功。

## 验证

`node --test tools/callback-test/meeting-recovery.test.cjs`：13/13通过。新增三项直接覆盖同意图新增重试并继续处理后续保存、连续失败预算及UUID/sequence冲突不冒认成功、重启保留重试状态；旧并发/退避/群隔离检查继续通过。仅虚构数据、无生产网络调用。

此时紧急原操作已实际恢复，预防补丁待部署回读。原问题的通用未知结果自动核验尚未实现；客户端草稿保留仍未取得真实验证，不能把本次恢复记作全部并发缺陷已解决。
