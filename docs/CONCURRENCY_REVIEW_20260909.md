# 多人使用自查｜2026-09-09

## 本轮修复状态（优先于下方原始审查快照）

User批准先修1/2并尝试核实3。1/2代码已修复并部署到两个正式群；10项针对性离线检查通过，包括审查中的微任务到达窗口、两群模式下200810恢复、超时不循环、重启续接和群隔离。worker结束重新检查；临时拒绝按1/2/5/15/60秒退避，之后最多每60秒再试原意图，保留UUID/sequence与排队正文。明确拒绝、未知结果各有持久状态及点击提示，不丢弃提交；不将UUID/sequence冲突当成功，不自动跳过未知请求。不可自动修复的参数/权限等拒绝仍需维护者处理，未承诺所有错误都自动恢复。

部署10:34真实CONNECTED、两群MEETING_READY，原消息11/8行恢复，未发送新卡或刷新布局；现场未人为触发200810，恢复分支证据为隔离的API替身检查。完整部署证据见[MEETING_ACCEPTANCE.md](MEETING_ACCEPTANCE.md)，复核入口为`node --test tools/callback-test/meeting-recovery.test.cjs`。

第3项未闭环：本机computer-use截图接口失败；只读文字可用，但点击报`coordinate input geometry is unavailable`，未能进行真实输入。已请User做一次保留草稿/他人新增的最小核实，User回答“现在不方便核实”。没有据此判断成功或失败，也没有代填员工记录。Codex待真实客户端反馈继续核实，确认仍有问题再修；4/5此次未扩展。当前增量交Review，不标记全项修复。Subagents: none。

## 原始只读审查快照

- Task：TASK-0028；审查基线：ecbedbc；执行者：Codex；Subagents: none。
- 自查结论：Needs changes。不是正式 Review 通过，也不将近期草稿标识补丁记为缺陷已解决。
- 范围：实际桌面运行代码的输入、提交、队列、AI、故障恢复、容量与诊断；不修改业务代码、配置、运行进程或群卡片。
- 证据：只读源码与白名单状态；在独立 Node 进程中调用同一 MeetingService，用虚构人员和内存状态、无网络 API 替身复核具体时序。没有读取员工正文、调用模型、模拟真实群成员点击或上传原始回调。

## 1. P1：队列结束边界的新请求可能无人处理（已本地复现）

位置：`tools/callback-test/meeting-service.cjs:244` 的 pump，以及155行开始的handle。

pump在processing=true时直接返回。drain发现队列为空后返回，到finally将processing改为false之间，新请求可能已入队并得到“已排队”；finally只调用pumpAi，没有再次检查普通请求队列。这个窗口中的请求不会自己唤醒处理，只能等之后另一条可受理操作或重启带动。

复核：第一人的ROW_SAVED之后，使用3或4层queueMicrotask安排第二人的handle，定向覆盖drain已结束而finally未完成的窗口。实际代码输出：第二次已受理=true、queued=1、processing=false、pending=false、saved=1。对照正常同步提交两个人：saved=2、queued=0。没有替换drain/apply实现，只有日志钩子安排输入到达时间。

这是可复现的代码时序缺陷，不证明前两天某条真实失败就是它。修复应在worker退出时重新检查可处理队列；结果未知的pending必须继续暂停，不能无条件循环唤醒导致忙等。建议优先修复两个群共用的这一段。

## 2. P1：明确的更新拒绝也会停住全群队列（已本地复现）

位置：`meeting-service.cjs:314`、162行，以及`meeting-schedule.cjs:105`附近的active槽位处理。

flush将所有非零返回都保留为UPDATE_UNCONFIRMED，不区分平台明确拒绝、可稍后重试、和网络结果未知。worker返回后没有恢复任务；后续新提交因pending被拒绝。调度器见active槽位已存在就跳过，连接恢复本身不会恢复队列。

复核：两个虚构提交已入队，API返回200810，再让替身恢复code=0。结果pending=true、queued=2、worker=false；没有再次调用恢复后的API，第三人的提交被拒绝。

官方[更新组件](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/cardkit-v1/card-element/update)将200810说明为交互期间暂时无法更新、应等待交互结束再尝试。本次没有声称线上已出现这个码。应分别处理明确可重试、明确失败和结果未知，增加有限恢复与对提交人的结果反馈；未知结果仍按原UUID/sequence核对，不能盲目生成新操作或跳过未确认的先前操作。

## 3. P1：未提交草稿仍没有经过证实的保留机制（未闭环风险）

位置：`meeting-card.cjs:27`、`meeting-service.cjs:147`、`meeting-api.cjs:33`。

输入仅在提交时进入后台，default_value仍为上次成功保存的内容。稳定element_id补丁消除了代码层组件身份不稳定的问题，但官方未承诺它能保留客户端草稿，也没有已取得的真实双人输入证据。新增、编辑切换、删除和AI汇总都会向同一共享卡片推送更新；不能只检查“新增”一种触发。

需要针对真实客户端确认：A输入未提交时，B新增/提交/删除，以及群2AI返回，A文字是否仍在。若仍会丢失，应重新选择能隔离编辑与共享展示的交互方式，不能继续只靠局部更新或标识推断安全。新的交互路线在确认平台支持和User体验后另行落实；本次不创建新产品方向或部署替代方案。

## 4. P2：布局实际容纳不下承诺范围上沿的20人（已本地复现）

位置：`meeting-card.cjs:90`、`meeting-service.cjs:170`。

最小空内容数据按现有最坏状态预算：手填交付模式18人198元素、19人207元素；AI交付模式19人196元素、20人205元素。因此当前群1最多18人、群2最多19人，还会受正文长度/30KB进一步限制。官方[卡片结构](https://open.feishu.cn/document/uAjLw4CM/ukzMukzMukzM/feishu-cards/card-json-v2-structure)限定每卡200元素。

保护逻辑会拒绝超额新增/提交，不是清空已保存记录；但“最多20人”的目标并未满足。应压缩布局元素或明确调整可用人数，不能取消容量保护。该限制在稳定标识补丁前就存在；本次未借审查扩大修改范围。

## 5. P2：失败证据及排队后结果反馈不足（代码已确认）

位置：`meeting-service.cjs:157`、261行、`session.cjs:15`、`control.ps1`的Check分支。

所有前置拒绝统一记录ACTION_REJECTED；排队后finishRequest删除请求、只记录结果码，未留下可查的最终失败状态或反馈到原提交者。每次启动又将脱敏status.log截断。进程Check只确认进程还活着，发现不了第1项的“队列有请求但worker没运行”。

这让员工只看到“已排队”后无结果，也让维护者无法从重启后记录区分空输入、旧版本、权限拒绝或处理失败。建议保留有界的脱敏结果记录（时间、操作类型、拒绝原因、数量及状态），并检查队列停滞；无需保存员工正文、身份、原始回调或完整日志。

## 当前状态与处理顺序

只读快照：群1为11行/11行已有内容，群2为8行/3行已有内容；两群pending=false、queued=0，AI pending/failed均为0。这是当时状态，不说明不存在上述条件性缺陷。

已有的本人/群/消息/行版本校验、AI结果submissionId核对及不同群独立队列，本次未发现新的直接绕过路径；没有据此宣称全面安全。正常不同人的两条提交在本地对照中均保存，范围止于该场景。

优先修复1、2，再明确验证或重新设计3；随后处理4、5。所有发现留在TASK-0028下继续，9:40计划、红框提交和两个群的现有卡片本轮均不变。
