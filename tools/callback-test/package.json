{
  "name": "earlymeeting-local-callback-probe",
  "version": "0.4.0",
  "private": true,
  "description": "Owner-controlled meeting rows and weekday per-group daily cards",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test:recovery": "node --test meeting-recovery.test.cjs",
    "test": "node probe.cjs --self-test && node --test probe.test.cjs send.test.cjs meeting.test.cjs",
    "start": "node probe.cjs"
  },
  "dependencies": {
    "@larksuiteoapi/node-sdk": "1.73.3"
  }
}
