{
  "name": "earlymeeting-local-callback-probe",
  "version": "0.2.0",
  "private": true,
  "description": "Local callback diagnostics only; no persistence, sending or card updates",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node probe.cjs --self-test && node --test probe.test.cjs",
    "start": "node probe.cjs"
  },
  "dependencies": {
    "@larksuiteoapi/node-sdk": "1.73.3"
  }
}
